{
  "name": "agentscape-bridge",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "OpenClaw gateway → scape BotSdk bridge",
  "scripts": {
    "dev": "bun run src/index.ts",
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@toon-format/toon": "^1.0.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/ws": "^8.5.13",
    "typescript": "^5.6.0"
  }
}
